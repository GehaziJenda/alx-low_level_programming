#include "main.h"

/**
 * mul - Checks
 * Description: 'Multiplies two numbers'
 * @a: character to evaluate
 * @b: character to evaluate
 * Return: a*b
 */
int mul(int a, int b)
{
	return (a * b);
}
